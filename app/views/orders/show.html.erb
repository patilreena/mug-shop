<p id="notice"><%= notice %></p>

<p>
  <strong>Total:</strong> <%= @order.total %></p>

  <%= @order.total %>
</p>

<p>
  <strong>User:</strong>
  <%= @order.user %>
</p>

<p>
  <strong>Product:</strong> <%= @order.product.name %></p>
    <img src="<%= @order.product.image_url %>">
  
</p>

<%= link_to 'Edit', edit_order_path(@order) %> |
<%= link_to 'Back', orders_path %>
